"""Configuration settings for document loader."""

# PDF Configuration
PDF_CONFIG = {
    "chunk_size": 1000,
    "chunk_overlap": 100,
}

# CSV Configuration
CSV_CONFIG = {
    "chunk_size": 500,
    "chunk_overlap": 50,
}

# HTML Configuration
HTML_CONFIG = {
    "chunk_size": 1000,
    "chunk_overlap": 100,
}

# TXT Configuration
TXT_CONFIG = {
    "chunk_size": 1000,
    "chunk_overlap": 100,
}

# DOCS Configuration
DOCS_CONFIG = {
    "chunk_size": 1000,
    "chunk_overlap": 100,
}

# General Configuration
CHUNK_SIZE = 1000
CHUNK_OVERLAP = 100
